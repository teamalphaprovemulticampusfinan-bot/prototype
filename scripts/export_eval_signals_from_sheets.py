from __future__ import annotations

print("This patch writes evaluation results through common.agent_history.save_run_result.")
print("Use the existing Google Sheet tabs agent_run_results / agent_run_result_chunks to inspect eval_chair_dma and eval_metrics records.")
print("If you need a local CSV, rerun scripts/run_eval_history_range.py with --no-sheets --export-local-csv <path> for a dry-run copy.")
